# Dev mock credentials (local only, VITE_AUTH_MOCK=true)
VITE_API_BASE_URL=http://127.0.0.1:8088
VITE_AUTH_MOCK=true
# Test accounts (also shown on /login when mock is enabled):
#   admin / admin       (ORG_ADMIN)
#   manager / manager   (MANAGER)
#   hr / hr             (HR_ANALYST)
#   auditor / auditor   (AUDITOR)
