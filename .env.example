# Environment variables (copy to .env — never commit .env)

# Application
# APP_ENV=development
# APP_PORT=3000

# Database
# DATABASE_URL=

# Auth / secrets (generate per environment)
# JWT_SECRET=
# ENCRYPTION_KEY=
